{
  "name": "L2LAAF Autonomous Orchestrator (DEV)",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "agent-bus-dev",
        "options": {}
      },
      "id": "bus-trigger",
      "name": "Agent Bus Trigger",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1,
      "position": [100, 400],
      "webhookId": "agent-bus-dev-hook"
    },
    {
      "parameters": {
        "model": "gemini-2.5-flash-preview-09-2025",
        "prompt": "=Analyze the following code change proposal:\nPath: {{ $json.body.payload.manifest.targetPath }}\nReason: {{ $json.body.payload.manifest.reason }}\n\nIs this change HIGH_IMPACT? A change is HIGH_IMPACT if it modifies tax logic, regulatory rules (HBR), authentication, or core system instructions.\nReturn ONLY a JSON object: { \"impact\": \"HIGH_IMPACT\" | \"ROUTINE\", \"justification\": \"string\" }"
      },
      "id": "impact-classifier",
      "name": "Impact Classifier",
      "type": "n8n-nodes-base.googleGemini",
      "typeVersion": 1,
      "position": [300, 400],
      "credentials": {
        "googleApi": { "id": "Gemini API Key", "name": "Gemini API Key" }
      }
    },
    {
      "parameters": {
        "conditions": {
          "string": [
            {
              "value1": "={{ $json.impact }}",
              "value2": "HIGH_IMPACT",
              "operation": "equal"
            }
          ]
        }
      },
      "id": "impact-switch",
      "name": "Impact Switch",
      "type": "n8n-nodes-base.if",
      "typeVersion": 1,
      "position": [500, 400]
    },
    {
      "parameters": {
        "model": "gemini-2.5-flash-preview-09-2025",
        "prompt": "=You are the L2LAAF Validator Agent (Red Team). Critically audit this proposed logic change:\nPath: {{ $node[\"Agent Bus Trigger\"].json.body.payload.manifest.targetPath }}\nProposed Logic: {{ $node[\"Agent Bus Trigger\"].json.body.payload.manifest.proposedLogic }}\n\nIdentify potential bugs, security flaws, or regulatory violations (CRA/AGLC). Be concise and strict."
      },
      "id": "validator-agent",
      "name": "Validator Agent",
      "type": "n8n-nodes-base.googleGemini",
      "typeVersion": 1,
      "position": [700, 300],
      "credentials": {
        "googleApi": { "id": "Gemini API Key", "name": "Gemini API Key" }
      }
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://chat.googleapis.com/v1/spaces/AAQAQxcghNA/messages",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={\"text\": \"🚀 **PROPOSAL FOR {{ $node['Agent Bus Trigger'].json.body.payload.manifest.targetPath }}**\n\n**Reason:** {{ $node['Agent Bus Trigger'].json.body.payload.manifest.reason }}\n**Impact:** {{ $node['Impact Classifier'].json.impact }}\n\n{{ $node['Validator Agent'] ? '🛡️ **VALIDATOR CRITIQUE:**\n' + $node['Validator Agent'].json.text : '✅ **ROUTINE CHANGE: No validator review required.**' }}\n\n[APPROVE](https://n8n.local2local.ca/webhook/approve?id={{ $node['Agent Bus Trigger'].json.body.id }}) | [REJECT](https://n8n.local2local.ca/webhook/reject?id={{ $node['Agent Bus Trigger'].json.body.id }})\"}"
      },
      "id": "chat-notify",
      "name": "Google Chat Card",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4,
      "position": [1000, 400]
    }
  ],
  "connections": {
    "Agent Bus Trigger": { "main": [[{ "node": "Impact Classifier", "type": "main", "index": 0 }]] },
    "Impact Classifier": { "main": [[{ "node": "Impact Switch", "type": "main", "index": 0 }]] },
    "Impact Switch": {
      "main": [
        [{ "node": "Validator Agent", "type": "main", "index": 0 }],
        [{ "node": "chat-notify", "type": "main", "index": 0 }]
      ]
    },
    "Validator Agent": { "main": [[{ "node": "chat-notify", "type": "main", "index": 0 }]] }
  }
}